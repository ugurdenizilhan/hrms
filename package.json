{
  "name": "hrms",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web": "npm run --prefix packages/web start",
    "mobile": "npm run --prefix packages/mobile start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "hrms",
    "react",
    "react-native"
  ],
  "author": "",
  "license": "ISC",
  "description": "Human Resources Management System for Web and Mobile",
  "dependencies": {
    "firebase": "^11.6.0"
  }
}
